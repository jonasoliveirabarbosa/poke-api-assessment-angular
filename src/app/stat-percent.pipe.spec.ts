import { StatPercentPipe } from './stat-percent.pipe';

describe('StatPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new StatPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
