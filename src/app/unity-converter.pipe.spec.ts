import { UnityConverterPipe } from './unity-converter.pipe';

describe('UnityConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnityConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
